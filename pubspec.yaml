name: movies_catalog
description: Turkcell technical challenge written with Flutter framwork in Dart language.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  animations: ^2.0.0
  cupertino_icons: ^1.0.2
  dio: ^4.0.0
  flutter:
    sdk: flutter
  flutter_mobx: ^2.0.0
  flutter_screenutil: 5.0.4
  flutter_svg: ^0.22.0
  get_it: ^7.2.0
  google_fonts: ^2.2.0
  json_annotation: ^4.0.1
  json_serializable: ^4.1.3
  lottie: ^1.1.0
  mobx: ^2.0.1
  mockito: ^5.0.10
  path: ^1.8.0
  path_provider: ^2.0.8
  provider: ^5.0.0
  sembast: ^3.1.2
  shared_preferences: ^2.0.6
  vexana: ^2.2.1
  webview_flutter: ^2.0.10

dev_dependencies:
  build_runner: ^2.0.4
  flutter_test:
    sdk: flutter
  lint: ^1.5.3
  mobx_codegen: ^2.0.1+3

flutter:
  uses-material-design: true

  assets:
    - assets/lang/
    - assets/images/
    - assets/svg/
    - assets/lottie/
